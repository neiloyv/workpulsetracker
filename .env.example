# Local development credentials — copy to .env and adjust if needed.
# Never commit .env with real secrets.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=workpulsetracker
POSTGRES_USER=workpulsetracker
POSTGRES_PASSWORD=workpulsetracker_dev_password

SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/workpulsetracker
SPRING_DATASOURCE_USERNAME=workpulsetracker
SPRING_DATASOURCE_PASSWORD=workpulsetracker_dev_password

# Google OAuth (Web client) — fill after creating credentials in Google Cloud Console
GOOGLE_CLIENT_ID=replace-me.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=replace-me

# Frontend origin for CORS + post-login redirect
APP_UI_ORIGIN=http://localhost:5173
SERVER_PORT=8080

# Placeholder download URLs for landing page
DOWNLOAD_WINDOWS_URL=/downloads/workpulsetracker-agent-windows.msi
DOWNLOAD_MACOS_URL=/downloads/workpulsetracker-agent-macos.dmg
DOWNLOAD_LINUX_URL=/downloads/workpulsetracker-agent-linux.deb
