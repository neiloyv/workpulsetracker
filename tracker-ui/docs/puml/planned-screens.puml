@startuml tracker-ui-pages
title tracker-ui — page flow

start
:Landing /;
if (logged in?) then (no)
  :Continue with Google;
  :/auth/callback;
endif
if (onboarded?) then (no)
  :/onboarding;
endif
:/org;
fork
  :Statistics tab;
fork again
  :Users tab;
fork again
  :Settings tab;
end fork
stop

@enduml
